{
  "name": "jupiteration",
  "version": "0.0.1",
  "description": "An tree-shakable iteration library.",
  "main": "dst/cjs/index.js",
  "module": "dst/es6/index.js",
  "types": "dst/es6/index.d.ts",
  "repository": "https://github.com/lazarljubenovic/jupiteration",
  "author": "Lazar Ljubenović <lazar.ljubenovic.1995@gmail.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build:esn": "tsc -p tsconfig.esn.json",
    "build:es6": "tsc -p tsconfig.es6.json",
    "build:cjs": "tsc -p tsconfig.cjs.json",
    "build": "yarn build:cjs && yarn build:esn && yarn build:es6",
    "test": "ts-mocha -p tsconfig.json tests/**/*.spec.ts",
    "test:w": "ts-mocha -p tsconfig.json tests/**/*.spec.ts -w --watch-extensions ts",
    "test:c": "nyc --reporter=html --reporter=text --reporter=text-summary yarn test"
  },
  "devDependencies": {
    "@types/chai": "^4.2.5",
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.12.7",
    "chai": "^4.2.0",
    "conditional-type-checks": "^1.0.4",
    "mocha": "^6.2.2",
    "nyc": "^15.0.1",
    "ts-mocha": "^6.0.0",
    "typescript": "^3.7.2"
  }
}
